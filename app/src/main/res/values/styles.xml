<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="parent_text_style">
        <item name="android:fontFamily">@font/lato</item>
    </style>

    <style name="team_name_style" parent="parent_text_style">
        <item name="android:textColor">@color/darkBlue</item>
        <item name="android:textSize">@dimen/team_name_text</item>
        <item name="android:layout_marginBottom">@dimen/text_view_margin</item>
    </style>

    <style name="country_name_style" parent="parent_text_style">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/country_name_text</item>
    </style>

    <style name="is_national_text_style" parent="parent_text_style">
        <item name="android:textColor">@color/darkGreen</item>
        <item name="android:textSize">@dimen/is_national_team_text</item>
        <item name="android:layout_marginTop">@dimen/text_view_margin</item>
    </style>
</resources>
